# ERPNext connection -- shared service account used for calls made with
# no per-user identity bound (system/internal calls). Any one of the
# three prefixes below works (multi-var fallback in ERP/erp_client.py).
ERP_URL=https://your-site.erpnext.com
ERP_API_KEY=your_service_account_api_key
ERP_API_SECRET=your_service_account_api_secret

# ERPNEXT_URL=...
# ERPNEXT_API_KEY=...
# ERPNEXT_API_SECRET=...
# FRAPPE_URL=...
# FRAPPE_API_KEY=...
# FRAPPE_API_SECRET=...

# LLM
OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini

# Web Search
# Tavily is the primary search engine (AI-optimized, designed for LLM agents).
# Get a free key at https://app.tavily.com (1000 searches/month free).
# If not set, the system falls back to the local SearXNG Docker container.
TAVILY_API_KEY=
# Override the SearXNG URL if you run it on a different port or host.
SEARXNG_URL=http://localhost:8080

# Voice (TTS/TTS.py + TTS/STT.py) -- both use OpenAI's hosted audio
# models via OPENAI_API_KEY above; no separate key needed.

# Voice (ws_voice.py)
# All voice I/O goes through OpenAI — no separate key beyond OPENAI_API_KEY above.
#
# TTS_MODEL: "tts-1" (low latency, good for voice chat) or "tts-1-hd" (higher quality).
TTS_MODEL=tts-1
# TTS_VOICE must be one of: alloy, ash, ballad, coral, echo, fable,
# onyx, nova, sage, shimmer, verse.
TTS_VOICE=alloy
# WHISPER_MODEL: "gpt-4o-mini-transcribe" (fast) or "whisper-1" (classic).
WHISPER_MODEL=gpt-4o-mini-transcribe

PORT=8050

# LangSmith tracing (optional)
LANGSMITH_TRACING=false
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=magma-assistance

# Audit log (SQLite)
# AUDIT_DB_PATH=magma_audit.sqlite

