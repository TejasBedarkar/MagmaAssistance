# Used via the PIP_CONSTRAINT env var (set in setup_and_run.bat).
#
# setuptools 81+ (Feb 2026) removed the pkg_resources module entirely.
# openai-whisper's setup.py still does `import pkg_resources`, and pip
# always fetches the newest setuptools for the throwaway build-isolation
# environment it spins up per source package -- regardless of what's
# installed in your venv -- so without this cap it grabs 81+ and the
# build fails with: ModuleNotFoundError: No module named 'pkg_resources'
#
# This constraint is picked up automatically for both the main install
# and each package's isolated build environment.
setuptools<81